'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.skills = exports.skillHelp = void 0;

var _react = _interopRequireDefault(require("react"));

var _ink = require("ink");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// plugin that provide help skill
const skillHelp = {
  name: 'help',
  help: 'help - list available skills',
  requirements: {
    adapters: ['cli']
  },
  rule: /HELP$/i,
  action: function (robot, msg) {
    const availableSkills = robot.responses.map(function (skill) {
      return skill.help ? `* ${skill.help}` : `* ${skill.name}`;
    });
    robot.sendComponent( /*#__PURE__*/_react.default.createElement(_ink.Text, null, `I have ${availableSkills.length} skills:\n${availableSkills.join('\n')}`));
  }
};
exports.skillHelp = skillHelp;
const skills = [skillHelp]; // should always return skills array

exports.skills = skills;